machine:
  pre:
    - sudo curl --output /usr/local/bin/phantomjs https://s3.amazonaws.com/circle-downloads/phantomjs-2.1.1
  node:
    version: 6.3.0

dependencies:
  cache_directories:
    - bower_components
    - node_modules
  override:
    - npm install

test:
  pre:
    - npm run preflight
